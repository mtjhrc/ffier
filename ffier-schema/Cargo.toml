[package]
name = "ffier-schema"
version = "0.2.0"
edition = "2024"
license = "Apache-2.0"
description = "JSON schema types for ffier FFI metadata"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
