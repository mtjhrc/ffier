[package]
name = "ffier-builtins"
version = "0.2.0"
edition = "2024"
license = "Apache-2.0"
description = "Pre-annotated built-in traits for ffier"

[dependencies]
ffier-rt = { path = "../ffier-rt" }
ffier-impl = { path = "../ffier-impl" }
