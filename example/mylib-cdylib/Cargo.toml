[package]
name = "mylib-cdylib"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
mylib = { path = "../mylib" }
ffier = { path = "../../ffier" }
ffier-bridge = { path = "../../ffier-bridge" }
ffier-bridge-macros = { path = "../../ffier-bridge-macros" }
