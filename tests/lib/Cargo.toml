[package]
name = "ffier-test-lib"
version = "0.1.0"
edition = "2024"

[features]
default = ["fruit-label"]
fruit-label = []

[dependencies]
ffier = { path = "../../ffier" }
foreign-trait-crate = { path = "../foreign-trait-crate" }
thiserror = "2"
