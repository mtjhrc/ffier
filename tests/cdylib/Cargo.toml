[package]
name = "ffier-test-cdylib"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[[bin]]
name = "gen-header"
path = "src/gen_header.rs"

[dependencies]
ffier-test-lib = { path = "../lib" }
ffier = { path = "../../ffier" }
