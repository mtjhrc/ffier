[package]
name = "ffier"
version = "0.2.0"
edition = "2024"

[features]
default = ["std"]
std = ["ffier-rt/std"]

[dependencies]
ffier-rt = { path = "../ffier-rt" }
ffier-annotations = { path = "../ffier-annotations" }
ffier-builtins = { path = "../ffier-builtins" }
