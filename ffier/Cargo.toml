[package]
name = "ffier"
version = "0.2.0"
edition = "2024"

[features]
default = ["std"]
std = ["ffier-rt/std"]

[dependencies]
ffier-rt = { path = "../ffier-rt" }
ffier-impl = { path = "../ffier-impl" }
ffier-builtins = { path = "../ffier-builtins" }
